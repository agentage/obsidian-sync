save-exact=true
legacy-peer-deps=false
# Obsidian releases require a bare version tag (no `v` prefix) — so `npm version`
# creates `0.1.0`, not `v0.1.0`. See .github/workflows/release.yml.
tag-version-prefix=
